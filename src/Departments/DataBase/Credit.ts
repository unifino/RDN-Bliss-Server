import { Client }                       from "pg"

// -- ======================================================================================

export const db = new Client( {
    user: 'RDN_Bliss',
    host: 'localhost',
    database: 'RDN_Bliss_Server',
    password: 'RDN',
    port: 5432,
} );

// -- ======================================================================================

db.connect();

// -- ======================================================================================
